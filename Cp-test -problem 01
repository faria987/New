#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    string a, b;
    cin >> a >> b;
    sort(a.begin(), a.end());
    sort(a.begin(), a.end());
    
    if(a == b) cout << "YES" << endl;
    else cout << "NO" << endl;
}
